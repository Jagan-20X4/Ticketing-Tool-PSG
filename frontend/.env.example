# Backend API URL (required when using PostgreSQL backend)
VITE_API_URL=http://localhost:4000

# Gemini API key for AI features (triage, suggestions). Optional – app runs without it.
GEMINI_API_KEY=your_gemini_api_key_here
